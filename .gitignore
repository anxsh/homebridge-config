# Homebridge runtime state (bridge identity, accessory cache, installed
# plugins, Google auth cookies, Config UI credentials/backups, and anything
# else the container writes into this directory) never belongs in version
# control. Ignore everything under config/ except the tracked template.
config/*
!config/config.json.example
